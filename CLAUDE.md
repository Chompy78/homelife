# Homelife — instructions for AI coding agents

The agent instructions are maintained in a single source of truth: **`AGENTS.md`** at the repo root.
Claude Code imports it automatically below — do not duplicate content here; edit `AGENTS.md` instead.

@AGENTS.md
